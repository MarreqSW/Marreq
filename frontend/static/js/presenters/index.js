/**
 * View model builders (presenters)
 * @module presenters
 * 
 * Pure functions that transform raw data into view models
 * suitable for rendering in templates or UI components.
 */

export {
  buildRequirementViewModel,
  statusBadge,
  verificationBadge,
  verificationPercent,
  solidity,
  reference,
  initials,
  purpose,
  notesAndAttachments,
  timeline,
  EMPTY_MESSAGE,
} from './requirement.js';
