{{!--
  Depends on: css/50-components/tables.css, js/pages/entityLogs.js
  Dynamic classes: badge bg-*
--}}
{{#*inline "page"}}
<div class="container mt-4">
    <div class="row">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h1>Logs for {{entity_type}} #{{entity_id}}</h1>
                <div>
                    <a href="/export_logs/{{entity_type}}/{{entity_id}}" class="btn btn-primary me-2">
                        <i class="fas fa-download"></i> Export Logs
                    </a>
                    <a href="/logs" class="btn btn-secondary">Back to Logs</a>
                    <a href="/admin" class="btn btn-outline-secondary">Admin</a>
                </div>
            </div>

            <div class="card">
                <div class="card-header">
                    <h5 class="mb-0">Activity History</h5>
                </div>
                <div class="card-body">
                    {{#if logs}}
                        <div class="table-responsive">
                            <table class="table table-striped">
                                <thead>
                                    <tr>
                                        <th>Timestamp</th>
                                        <th>User</th>
                                        <th>Action</th>
                                        <th>Description</th>
                                        <th>IP Address</th>
                                        <th>Details</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    {{#each logs}}
                                    <tr>
                                        <td class="datetime-cell" data-timestamp="{{created_at}}">{{created_at}}</td>
                                        <td>{{username}}</td>
                                        <td>
                                            <span class="badge action-badge" data-action-type="{{action_type}}">
                                                {{action_type}}
                                            </span>
                                        </td>
                                        <td>{{description}}</td>
                                        <td>
                                            <small class="text-muted">{{ip_address}}</small>
                                        </td>
                                        <td>
                                            {{#if old_values}}
                                                <button
                                                    class="btn btn-sm btn-outline-info"
                                                    type="button"
                                                    data-action="show-changes"
                                                    data-old-values="{{old_values}}"
                                                    data-new-values="{{new_values}}"
                                                >
                                                    View Changes
                                                </button>
                                            {{/if}}
                                        </td>
                                    </tr>
                                    {{/each}}
                                </tbody>
                            </table>
                        </div>
                    {{else}}
                        <div class="text-center text-muted">
                            <p>No logs found for this entity.</p>
                        </div>
                    {{/if}}
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Modal for showing changes -->
<div class="modal fade" id="changesModal" tabindex="-1">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Changes</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <div class="mb-3">
                    <small class="text-muted">
                        <span class="text-danger">🔴 Red</span> = Removed/Changed values | 
                        <span class="text-success">🟢 Green</span> = Added/New values | 
                        <span class="text-muted">⚪ Gray</span> = Unchanged values
                    </small>
                </div>
                <div id="changesContent">
                    <!-- Content will be loaded here -->
                </div>
            </div>
        </div>
    </div>
</div>

{{/inline}}

{{> layout page_id="entity-logs"}}
