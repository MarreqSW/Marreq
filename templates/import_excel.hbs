{{#*inline "page"}}
<div class="container mt-4">
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <div class="card c-card">
                <div class="card-header c-card__header">
                    <h3>Import File</h3>
                </div>
                <div class="card-body c-card__body">
                    <p>Upload a file to import requirements or tests into the current project.</p>
                    <form action="/p/{{selected_project_id}}/import_excel/upload" method="post" enctype="multipart/form-data">
                        <div class="mb-3">
                            <label for="excel_file" class="form-label c-form-label">Select File</label>
                            <input type="file" class="form-control c-form-control" id="excel_file" name="file" accept=".xlsx,.xls,.csv" required>
                            <div class="form-text c-form-text">Supported formats: .xlsx, .xls, .csv</div>
                        </div>
                        <button type="submit" class="btn btn-primary">Upload and Map Columns</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
{{/inline}}
{{> layout this}} 