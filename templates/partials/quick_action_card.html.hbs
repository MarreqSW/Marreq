{{!--
  Expected parameters:
    href: target URL (required unless disabled)
    icon: icon class name
    label: button label
    modifier: optional modifier class (e.g., reqman-action-card--primary)
    disabled: truthy value marks the card as disabled
    title: optional tooltip/title attribute

  Depends on: css/50-components/dashboard.css
--}}
{{#if disabled}}
  <span
    class="reqman-action-card{{#if modifier}} {{modifier}}{{/if}} reqman-nav-link--disabled"
    aria-disabled="true"
    {{#if title}}title="{{title}}"{{/if}}>
    {{#if icon}}
    <i class="{{icon}}" aria-hidden="true"></i>
    {{/if}}
    <span>{{label}}</span>
    {{#if badge}}
      <span class="reqman-badge">{{badge}}</span>
    {{/if}}
  </span>
{{else}}
  <a
    href="{{href}}"
    class="reqman-action-card{{#if modifier}} {{modifier}}{{/if}}"
    {{#if title}}title="{{title}}"{{/if}}>
    {{#if icon}}
    <i class="{{icon}}" aria-hidden="true"></i>
    {{/if}}
    <span>{{label}}</span>
    {{#if badge}}
      <span class="reqman-badge">{{badge}}</span>
    {{/if}}
  </a>
{{/if}}
