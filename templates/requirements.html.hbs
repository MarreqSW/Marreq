{{#*inline "page"}}
<div class="mb-4">
  <div class="d-flex justify-content-between align-items-center">
    <h1>Requirements</h1>
    <div>
      <a href="/requirements.xls" class="btn btn-success">Export Excel</a>
      <a href="/new_requirement" class="btn btn-primary">New Requirement</a>
    </div>
  </div>
</div>

<!-- Filters Section -->
<div class="card mb-4">
  <div class="card-header">
    <h5 class="mb-0">Filters</h5>
  </div>
  <div class="card-body">
    <form method="GET" action="/requirements" class="d-flex align-items-end gap-3">
      <div class="flex-grow-1">
        <label for="status_filter" class="form-label">Status</label>
        <select class="form-select" id="status_filter" name="status_filter">
          <option value="">All Statuses</option>
          {{#if statuses}}
            {{#each statuses}}
              <option value="{{st_id}}" {{#if (eq st_id ../current_status_filter)}}selected{{/if}}>
                {{st_title}}
              </option>
            {{/each}}
          {{/if}}
        </select>
      </div>
      <div class="flex-grow-1">
        <label for="verification_filter" class="form-label">Verification Mode</label>
        <select class="form-select" id="verification_filter" name="verification_filter">
          <option value="">All Verification Modes</option>
          {{#if verifications}}
            {{#each verifications}}
              <option value="{{verification_id}}" {{#if (eq verification_id ../current_verification_filter)}}selected{{/if}}>
                {{verification_name}}
              </option>
            {{/each}}
          {{/if}}
        </select>
      </div>
      <div class="flex-grow-1">
        <label for="category_filter" class="form-label">Category</label>
        <select class="form-select" id="category_filter" name="category_filter">
          <option value="">All Categories</option>
          {{#if categories}}
            {{#each categories}}
              <option value="{{cat_id}}" {{#if (eq cat_id ../current_category_filter)}}selected{{/if}}>
                {{cat_title}}
              </option>
            {{/each}}
          {{/if}}
        </select>
      </div>
      <div class="d-flex gap-2">
        <button type="submit" class="btn btn-primary">Apply Filters</button>
        <a href="/requirements" class="btn btn-secondary">Clear</a>
      </div>
    </form>
  </div>
</div>

<section id="Requirements">
  {{#each requirements }}
    {{> requirement}}
  {{/each}}
</section>
{{/inline}}
{{> layout}}